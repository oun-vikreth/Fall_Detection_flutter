import 'package:flutter/material.dart';

class AlertEmailRegistration extends StatelessWidget {
  const AlertEmailRegistration({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Alert Email Registration',
        style: TextStyle(color: Color.fromARGB(255, 0, 0, 0), fontSize: 40),
      ),
    );
  }
}
